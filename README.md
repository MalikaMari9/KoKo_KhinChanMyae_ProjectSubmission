⚠️ Sensitive values (AWS account ID, ARNs, Secrets) have been replaced with placeholders.

Before deploying:

Replace ACCOUNT with your AWS account ID
Replace REGION with your AWS region
Replace EXAMPLE with real cluster / secret names
